# macOS
.DS_Store
**/.DS_Store

# Playwright logs (если случайно появятся)
.playwright-mcp/

# Большие исходники дизайна (опционально — раскомментировать если репо распухнет)
# design/fills/editable-ai/

# Высокорезные фоны (для печати, не для лендинга)
# assets/фон_*.png
